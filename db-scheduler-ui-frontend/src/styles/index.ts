import colors from './colors';
import typography from './typography';
import breakpoints from './breakpoints';
import components from './components';

// All styles are placeholders added as examples
const theme = {
  colors,
  typography,
  breakpoints,
  components,
};

export default theme;
